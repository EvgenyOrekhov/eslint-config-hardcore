name: CI

on:
  push:
    branches:
      - master
  pull_request: null

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "20"

      - run: npm ci

      - run: npm test
